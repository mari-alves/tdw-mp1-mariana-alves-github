pre-commit:
  parallel: true
  commands:
    linter:
      run: npm run lint
    prettier:
      run: npm run prettier
    tests:
      run: npm run test

pre-push:
  parallel: true
  commands:
    linter:
      run: npm run lint
    prettier:
      run: npm run prettier
    tests:
      run: npm run test
